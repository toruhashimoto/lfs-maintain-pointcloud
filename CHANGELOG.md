# Changelog

## 1.0.1 (2026-08-03)

Input Crop パネルの GUI 実機検証で見つかった問題の修正。パネル表示・箱の
配置・調整（ギズモ/数値）までは実機で確認済み。Export → クロップ済み学習の
一巡は次回の実用検証で行う。

### fix: プラグインがロードされない（`uv not found`）

`pyproject.toml` の mtime が `.venv/.deps_installed` より新しいと、ローダが
依存の再同期を試みる。**ソースビルドの LichtFeld は uv を同梱せず、システム
uv も設計上拒否するため、この経路は必ず失敗し、パネルが 1 枚も出なくなる**
（v0.4.0 のバージョンスタンプ自体が引き金だった）。対処と恒久策（ローダ
探索先への uv 配置）は HANDOFF「プラグイン設置の注意」。

### fix: Input Crop の箱が調整できない

エンジンのクロップ箱ギズモは**選択駆動**（NodeSelected ハンドラが
`builtin.cropbox` ツールを起動する）で、パネルは箱を作るだけで選択して
いなかった。Seed / Fit 直後に `lf.select_node()` で自動選択し、その場で
ドラッグできるようにした。

### 追加: 箱のリサイズ操作系（SuperSplat 相当）

- **Move (gizmo) / Scale (gizmo)** ボタン — `lf.ui.set_active_operator` で
  ギズモ種別を直接切替（メインツールバーの移動/スケールと同じ経路）
- **Center + Size** の数値表現を min/max と併設。書き込みは箱の min/max
  データへ直行するため、**書き出しと必ず一致する**
- **Shrink 5% / Expand 5%**（中心固定の等比）
- ギズモのドラッグが反映されているかはパネル数値の追従で確認できる旨を明記

### fix: GUI プラグインと `--python-script` の二重登録ガード

`load_on_startup: true`（GUI 設置）とキャンペーンの `--python-script` を
併用すると AnchorRegularizer が二重登録され毎 iteration 二重に引かれる。
`headless_anchor.py` がロード済みプラグインを検出したらその regularizer を
再利用しフックを張らないようにした（env 設定経路は維持）。テスト +2 本で
**132 本**。

## 1.0.0 (2026-08-01)

最初の安定版。**コードは 0.4.0 と同一**で、位置アンカー・入力クロップの両機能と
既定値（`calibrate_quantile=60`、プラグイン自体は既定オフ）をこの版の仕様として
確定する。両機能とも実データの A/B（4 データセット・のべ 60 本以上、各アーム
2 本以上）で効果を検証済み（docs/RESULTS.md）。

- README を公開向けに全面刷新（プラグイン説明を主体に、開発経緯は docs/ へ委譲）
- docs/HANDOFF.md を統合・現行化（「現在地」節を新設）
- pyproject の description を入力クロップを含む現行機能に合わせて更新

## 0.4.0 (2026-07-31)

### 追加: Input Crop パネル — GUI からの手動入力クロップ

RealityScan の再構成領域に相当する操作をビューア上で提供する。エンジン
native のクロップ箱ノード＋標準ギズモをそのまま使い（パネル独自のギズモは
持たない）、パネルは「既定 ROI/点群フィットで箱を配置 → サンプリング計数
（1/8、約 1 秒）→ 厳密な全点パスで `<data>_cropped` を書き出し」を担う。
書き出しは CLI (`crop_input.py`) と同一実装。

- 長時間処理（フィット・計数・書き出し）はワーカースレッドで実行し、
  draw() を凍結させない（UI スレッドで 14 秒止まる事故の防止）
- 書き出しは軸平行 min/max のみ（箱ノードの回転は反映されない旨をパネルに
  明記）。inverse（外側保持）の箱は書き出し拒否 — 被写体を消す事故防止
- 純ロジックは既存モジュールに追加してテスト（`fit_box` / `box_to_text` /
  `scan_points3d(sample_every)` / `read_xyz_sample`、+5 本で計 130 本）。
  パネル自身は薄いグルーに保つ
- `lf.dataset_params().data_path` で COLMAP パスを自動取得（手入力可）

### 既定変更: `calibrate_quantile` 70 → 60（2026-07-31、利用者承認）

**v0.1.0 以来はじめての既定値変更。** 位置忠実性が第一優先という方針確定
（README「位置づけ」節）を受け、2 データセットで一致した掃引結果に基づく。

- q60 は q70 より drift p95 をさらに 7〜12% 締め（B: −44.9→−52.4%、
  C: −43.3→−50.1%）、測光コストは検出されないまま。増分の代償は
  scale_p90 +0.8pt（両データセットで一致）
- **q50 は既定にしない**: 形状コストの増分が倍（+1.3〜+1.7pt）、C で
  q70 比の SSIM に初めて統計的に見える負号（−0.3%、効果/ノイズ 5.0）、
  かつ掃引した測定領域の端。既定は両隣が実測済みの点に置く。
  計測がシビアな案件は per-run で `LFS_MPC_CALIB_Q=50`（掃引済み範囲内）
- 既定を固定するテストを追加（`mpc_tests/test_defaults.py`。ホスト専用
  モジュール `lichtfeld` は import 時スタブで満たし、実クラスの実既定値を
  検証する。「既定オフ」の方針コミットメントも同時にピン）
- `run_campaign.ps1` のフォールバックも 60 に追従。**既存プランの校正腕
  34 本には `q = 70` を明示ピン** — これらは旧既定で走って記録された腕で、
  再実行時に意味が変わってはならない（`analyze_campaign.py` の
  `datac_q70` ラベル等が指すものと一致し続ける）

### 入力点群クロップ（`crop_input.py`）

学習前に `points3D.txt` を箱でフィルタした COLMAP データセットを作る CLI。
3 つ目のデータセット（C）で 32 本回して測った結果、**手作業クリーンアップを
減らす手のうちこれだけが実用的**だったため、前回手書きスクリプトで済ませていた
手順を機能として起こした（箱外スプラット −56.0%、効果/ノイズ 15.5、納品領域の
PSNR は −0.24 dB。プラグインの学習時機能で最良のものは −8.6%）。

- `--dry-run` で 1 点も書かずに除去数だけ出す。**箱を決める作業がこれになる**
- `cameras.txt` / `images.txt` はハードリンク、`images/` はジャンクション。
  追加ディスクは `points3D.txt` の分だけ（4.1M 点で 415 MB、14 秒）
- `# Number of points:` を実際の残数に書き直す。放置すると**学習は通るのに
  以後その数から導く値がすべて狂う**という静かな失敗になる
- 使った箱を出力先の `crop_input.json` に残す
- 箱の指定は既存の `cropbox.py`（`parse_box` / `pad_box`）を再利用。余白は既定 5%
- `LFS_MPC_CROP_BOX`（**学習中の**クロップ箱、納品領域が 3.71 dB 悪化するため
  使ってはいけない）とは別機能。混同しないこと

**バイナリモードで読み書きする。** COLMAP は Windows でも LF で書くため、
テキストモードだと 437 MB のファイルが全行 CRLF に変換される。

**`images.txt` の POINT3D_ID は付け替えない。** 落とした点への参照が残るが、
COLMAP のリーダは宙ぶらりんの参照を許容し、LichtFeld Studio は点群を初期化に、
カメラを姿勢にしか使わない（C のクロップ済みデータセットは問題なく学習できた）。
479 MB の `images.txt` を書き直す価値はない。**将来リーダが弾いたらここを見ること。**

テストは `mpc_tests/test_crop_input.py` に 26 本（合計 **119 本**）。
実装中に 2 件の実害バグを検出した: `--box -1,...` を argparse がオプションと
誤認する件（**実際の箱はすべて負の座標で始まる**）と、`mklink /J` が
スラッシュ区切りのパスを拒否する件（POSIX 風シェルから呼ぶと必ず踏む）。

### 公称箱の座標を訂正（2 データセットの納品モデルで確定）

COLMAP フレームでの公称箱は **`-1,-1.5,-1:3,0.5,1`**（中心 `[1,-0.5,0]`・
サイズ `[4,2,2]`）。**それまで記録されていた中心 `[-1,0.5,0]` は x と y の符号が
逆だった。** そのまま当てると データセット D の点群の 27.1% が落ち、被写体が
箱に入らない（符号を直すと 7.7%）。

上の公称箱は C と D 両方の実測箱を内包する。**z は 2 データセットとも両面で
差が 0.000〜0.0001**、x 下限も D では 0.001 差。規約は実在する。ただし C は
hi 側で 0.120 超えるので余白 5% はそのまま必要。

### 2 つ目の被写体（データセット D）で箱の規則が再現

納品モデル（`splat_109800.ply` 生 5,000,000 / `splat_109800b.ply` 手作業済み
4,568,467）が揃ったため、C と同じ「専門家のクリーンアップを正解ラベルにする」
評価を実施。**除去 431,533 行のうち 80.3% が納品箱の外**（C は 74.8%）。

ただし**同じレジームではない**。この違いは今後の解析の前提になる。

| | C | D |
|---|---|---|
| 手作業で除去 | 1.53% | **8.63%**（431,533 行） |
| うち箱の外 | 74.8% | 80.3% |
| 箱内除去の基底率 | 0.39% | **1.823%** |
| 撮影点のうち箱の外 | 0.68% | **10.612%** |

**箱内除去の基底率が 4.7 倍高い**ので、「スプラット単体の特徴で箱内除去を予測
できるか」（C では適合率 4〜16% で失敗）はこのデータの方が検証しやすい。
**撮影点の 10.6% が箱の外**にあり、C の 0.68% の 15 倍。入力クロップの働く
余地はこちらが大きい。

キャンペーン定義に `-Plan datad` / `-Plan datadcrop` を追加（各 4 本、
`cap 5000000` は operator が実際にクリーンアップした実行に合わせてある）。

`scripts/removed_split.py` を追加。生と手作業済みの PLY を float32 XYZ の完全一致で
照合して除去行を特定し、箱の内外に分ける。**「保持行が箱の外に 1 行も無い」は
箱を保持行の bbox として定義しているので自明**であり、根拠として使ってはいけない
（整合チェックとしてだけ出力する）。根拠になるのは除去のうち箱が説明する割合。

### 検証: 入力クロップは 2 つ目の被写体で再現（D, 8 本 + スモーク）

`-Plan datad` / `datadcrop` を 30,000 iter × 8 で実行し、operator の納品モデルを
正解ラベルとして評価（詳細は [docs/RESULTS.md](docs/RESULTS.md)）。

- **箱外スプラット −59.1%（効果/ノイズ 51.1）。** C の −56.0%（15.5）を再現し、
  切り分けはより明瞭
- **箱内 PSNR は +0.11 dB（3.7）でコストが出なかった**（C は −0.24 dB）。
  2 データセットを通して「コストは −0.24〜+0.11 dB の帯、いずれも小さい」
- **クロップ済み入力に `nn` + `anchor_new` を重ねない。** 箱外の追加効果は
  効果/ノイズ 1.76 / 1.3（2 データセットとも結論未満）で、箱内 PSNR は −1.04 / −0.29 dB と
  方向一致のコストが出る。README の推奨を「クロップのみ」に更新
- 未クロップでの `nn` は健在（−2.4%、4.3）。撮影面への吸着（箱内 p99 距離
  3.6 → 1.0 点間隔）も再現

### 検証: novel-view 比較で「見た目」との架橋が決着（`scripts/novelview.py`）

v0.1.0 から未解決だった「hold-out PSNR が平坦なのに見た目は良くなるのか」を
リング外視点の描画で決着させた。エンジンの `--render-camera-path`（ヘッドレス）で
同一軌道を 5 モデル（base / nn / crop_base / crop_nn / 納品クリーン）に描画し、
63 視点で箱外の発光ピクセルと孤立フローターを計数。

- **孤立フローターはクロップで −91%**（46.3 → 4.1 /視点）。真上視点では base が
  もやに埋まるのに対しクロップは車体が見える
- **nn は見た目に寄与しない**（base と完全に同水準）— アンカーの価値は位置忠実性
  という方針決定と整合
- クロップは**リング近傍の背景を「風景」から「にじみ」に変える**（種点を失った
  空間の成長は写真の裏付けを持たない）。納品形（黒背景に車体）には手作業が
  引き続き必要 — クロップは量を減らす（−59%）が置き換えない
- hold-out PSNR は 4 腕すべて ~21.5 dB で並んだまま — **平坦だった理由は
  「差が無い」ではなく「差が見える視点を測っていない」**

ツール: `novelview.py path`（軌道 JSON 生成。回転はカメラ→ワールドの wxyz、
焦点は 35mm 換算・SENSOR_HEIGHT 24 — エンジンのソースと突き合わせ済み）、
`count`（箱の投影ハル外の計数。キーフレームをフレームに整数対応させ、C++ の
スプライン補間を再実装せずに姿勢を厳密に得る）、`collage`。
フレーム抽出は `scripts/mp4frames.c`（同梱 FFmpeg 8 DLL に対して VS で
ローカルコンパイル。プロキシが認証必須で何も落とせない環境への対処）。

**罠 2 件**: エンジンの timeline リーダはサイズをバイト数で検査しつつテキスト
モードで読むため、**camera path JSON を CRLF で書くと「読んでいる間にサイズが
変わった」と拒否される**（LF で書く。COLMAP と同じ規律）。argparse の負値問題は
novelview の `--heights -2.2,...` でも踏む（crop_input と同じ対処を実装）。

### 検証: q 掃引は C でも単調（`-Plan datacq`, +4 本）

未解決だった「q・スケール膨張のデータセット依存性」を閉じた。対ベースラインで
drift p95 は q70/q60/q50 で −43.3% → −50.1% → −56.1%（効果/ノイズ 94〜345）、
scale_p90 は +2.7% → +3.5% → +4.5%。データセット B と数値までほぼ並行で、
**「締めるほど位置は単調に良くなり、代償は形状の裾に出る」は機構の性質**。
測光コストは基準比で今回も検出されず、スケール膨張は中央値に出ない（裾のみ）。
既定 q=70 は据え置き（締めるかは位置と形状のトレードオフで運用判断）。

### 箱内除去のデータセット横断予備解析（`scripts/inbox_rule.py`）

2 データセットのラベルで per-splat 特徴を評価。**特徴の序列はデータセット間で一致**
（箱境界への近さ・撮影点群の局所密度が両データセットのトップ2）するが、
**絶対閾値は移転しない**。そのモデル自身の分布による**分位点ルール
（face+dens8 の上位 5% をフラグ、ラベル不要・チューニングゼロ）が箱内除去の
51.1% / 53.2% を捕捉**する。精度は基底率なり（4.0% / 19.4%）のため
自動削除ではなくレビューの絞り込み用。n=2 データセットの予備結論。

## 0.3.0 (2026-07-30)

**0.2.1 として書いた修正はこのリリースに含まれる**（main にマージ済みだが
単独でタグは打たなかった）。下の 0.2.1 節も 0.3.0 の内容として読むこと。


不感帯を「点群の点間隔の倍数」ではなく「自由ドリフト分布の分位点」で決める
自己校正機構を追加。**既定はオフで、既定値は一切変えていない。**

**なぜ**

2 つのデータセットで最適な `free_radius_spacing` が 2.0 と 1.0 に割れており、
HANDOFF は既定を決めるには 3 つ目のデータセットが要ると記録していた。既存結果を
「不感帯を自由ドリフト分布のどのパーセンタイルに置いたか」で表し直すと、効いた
2 条件はどちらも p55〜p75 の帯にあり、効かなかった 1 条件だけが p95 の外にあった。
**倍率では一致しないものが分位点では一致する** — 不感帯は点群密度ではなく
ドリフト分布に対して決めるべき量だった。

その分布は学習開始時に存在せず、学習中に約 5 倍成長するため、実行中に測る必要がある。

**追加**

- **対照行。** 初期行の `control_fraction`（既定 2%）を学習中ずっと引かない行として
  確保する。対照行の分位点が、任意の iteration における自由ドリフト分布の実測推定に
  なる。選択は `calibrate_seed` で決定論的。引き戻しからの除外は**キャプチャ時に
  `_mask` から差し引くだけ**で行い、`apply()` の引き戻し数式には触れていない。
- **校正。** `calibrate_every`（既定 500）iter ごとに
  `free_radius = clip(quantile(対照行ドリフト, calibrate_quantile), r_min, r_static)`。
  `r_static` は現行の静的規則で**上限**として働くため、校正は今より締まる方向にしか
  動けない。`r_min = calibrate_min_spacing × 点間隔`（既定 0.25 倍）は下限で、
  不感帯 0 と固定 strength の組み合わせ（終盤の完全凍結）を防ぐ。変化が 5% 未満なら
  適用しない。`free_radius` を明示指定した場合は常にそちらが勝つ。
- **q の決定を掃引にしない。** 対照行のドリフトを q ∈ {50,55,60,65,70,75,80,90,95}
  すべてで記録し、適用するのは設定した 1 つだけ。ソート 1 回のコストで全実行から
  q カーブが得られるため、q の決定が実行計画ではなく事後解析になる。
- **形状統計。** `exp(scaling_raw)` の最大軸分位点と占有体積プロキシを出力
  (`scale`)。位置は改善しつつスケールが膨らむ代償を、ドリフト値と並べて見るため。
- **統計 JSON の拡張。** `control_drift`（対照行の分布と q カーブ）、
  `free_radius_history`（校正の軌跡）、`scale`。`init_drift` は**対照行を除外**して
  報告する（除外しないと、刈るべき上位の裾を対照行が汚染する）。
- `scripts/run_campaign.ps1`（直列ランナー・VRAM 監視・失敗継続・再開可能）、
  `scripts/collect_results.py`、`scripts/analyze_campaign.py`（効果÷ノイズ解析）。

**検証（データセット B, 30000 iter, 15 本）**

ベースライン 6 本・校正 2 本・固定 `spacing=1.0` 2 本・`--max-cap 16M` 4 本。

- **校正は手動掃引の結果を再現し、上回った。** ドリフト p90 −32.9%（効果/ノイズ 75.2）、
  p95 −44.9%（72.3）。同一キャンペーン内の固定 `spacing=1.0` 腕は −30.5% / −43.2% で
  過去の手動掃引を再現しており、校正との直接比較でも p50 −9.0%（220）、p75 −8.7%（53.4）。
- **測光コストは検出されない。** hold-out PSNR +0.08 dB（効果/ノイズ 0.4）、SSIM 0.2。
- **正しい不感帯は点群だけでは決まらない。** `--max-cap` を 8M → 16M にして密度制御の
  飽和を解くと自由ドリフトが 26.2% 広がり、校正は不感帯を 0.00321 → 0.00387 と
  21% 広げて追随した。点間隔から決まる固定値にはできない挙動で、高 cap でも効果は
  再現する（p90 −34.1%／40.1、p95 −47.1%／62.5）。校正 4 本すべてで
  p95 が不感帯の 0.999 倍に着地。
- **対照行のバイアスを実測（未補正）。** 対照行は 98% がアンカーされた場にいるため
  余分に動く。真のベースライン 6 本比で p50 +3.4% / p95 +7.7%。ベースラインの
  アーム内ばらつき（約 1e-5）の 47 倍で実在する。校正はこの分だけ広めを選ぶ。
- **スケール膨張はこのデータセットでは再現しなかった。** RAMESSES/Chest の
  中央値 +4.6%（効果/ノイズ 22）に対し **+0.3%（1.0）**。上位の裾のみ（p90 +2.1%／7.3）。
  位置アンカーのスケール代償はデータセット依存。

**判明した環境事実（HANDOFF の記載から変わっている）**

- Issue #1439（master で headless + `--python-script` が動かない）は**解消済み**
- `training_end` フックは**発火する**（v0.5.1 では未発火）。定期スナップショットは保険として維持
- `plugin check` の `ValueError` は出ない（exit 0 / `OK`）
- VRAM は 8M cap・SH 次数 3 で **7.3 GB**。docs の 17.1 GB という前提は v0.5.3 では成立しない
- **`--max-cap 8000000` は模型を縛っていた。** 追加行 113,668 に対し 16M では 8,113,668、
  hold-out PSNR は @7,000 で 16.76 → 22.20、@30,000 で 23.67 → 24.91

**追加検証（データセット B, さらに 12 本、計 27 本）**

- **q 掃引（70 / 60 / 50 各 2 本）。締めるほど位置忠実性は単調に向上し、
  測光コストは現れない。** p95 −44.9% → −52.4% → −57.9%。**代償は測光ではなく
  スプラット形状**で、`scale_p90` が +2.1% → +2.9% → +3.4% と単調に増える
  （いずれも有意）。既定 70 は変更していない。
  対照行が記録した q カーブから、**既定の静的規則（`spacing=2.0`）が不感帯を
  自由ドリフト分布の q95 に置いていた**（＝引く対象が無かった）ことと、
  **手動掃引が見つけた最良値が q70 だった**（比 1.01）ことが分かる。
- **`calibrate_start` は 1000 のままでよい。** 5000 は p50 +6.5%（効果/ノイズ
  13.1）、p75 +2.5%（6.4）と分布の胴体が悪化し、裾は変わらない。最終的な不感帯は
  ほぼ同じなので、差は早期の締め付けが蓄積を抑えた分。
- **対照行バイアスの機構を特定。** 強く締めるほど増え（q70/60/50 で
  +7.69/+8.71/+10.04%）、長く締めるほど増え（start=5000 で +4.38%）、自由な行が
  多いほど減る（対照行 10% で +5.81%、高 cap で +4.97%）。3 方向すべてが
  「アンカー行が吸収しなくなった残差を自由な行が肩代わりする」という予測どおり。
  **誤差は安全側**（不感帯を広めに選ばせるが、締めるほど良いので緩い側に外れる）。
  `control_fraction` を上げる対処は**非推奨**: 対照行は最終モデルで正則化されない
  ので、0.10 にすると置いた点群の 10%（789,000 行）が保持されない。

**新しく見えた 2 つの問いへの回答（さらに 4 本）**

- **高 cap で極端な裾を取り逃す理由 → 仕様どおりだった。** 上位 32 行を直接
  観測する診断（`tail_diag`）を追加して測ると、**31/32 と 32/32 が
  `min_pull_opacity`（0.01）未満**（不透明度 0.004〜0.009）で、設計上まったく
  引かれない行だった。`residual == drift` が 32/32 なので再配置の検出漏れでもない。
  16M cap は密度制御が本格稼働するぶん瀕死の行が桁違いに多く、ゲートを抜ける行が
  増える。**これらは不可視でレンダリングに寄与しないので実害はなく、汚染されるのは
  統計だけ。** 対処として `visible_drift`（`min_pull_opacity` 以上の行だけの分位点と
  逸脱行数）を併記。既存の `init_drift` は公開済みの表との比較可能性のため残した。
  **効果は決定的で、校正実行の `escaped_2x` は全行で 34 だが可視行では 0**。
  逸脱していたのは全て設計上引かない不可視行で、リーシュは責任を持つ行を
  100% 保持していた。ベースラインは逆に 207,593 行のうち 204,743 行（98.6%）が
  可視行＝本物のドリフト。**204,743 対 0。**
- **スケール膨張の原因は確認、しかし解消は割に合わない。** `reanchor_on_split`
  （既定 False）で分割行を分割後の位置に再アンカーすると `scale_p90` が −1.9%
  （効果/ノイズ 11.5）、ベースライン比の膨張が +2.8% → +0.8% になり、**DESIGN.md が
  特定した long-axis split との干渉が原因の約 2/3 を占めることが実証された**
  （1 実行あたり約 183 万行を再アンカー）。だが `drift_p999` +1179%、
  `escaped_2x` が 25 → 106,080 行（アンカー無しの 208,899 の半分）まで戻る。
  分割のたびにアンカーが動き、繰り返し分割される行に変位が累積するため。
  **寸法精度が目的なら成立しない取引で、既定は False のまま。**
  次に試すならアンカーを動かさない方向（分割直後の数イテレーションだけ引力を止める等）。

**既知のコスト**

- スナップショット 1 回あたりの GPU→CPU 転送が増えた。`init_drift` /
  `control_drift` / `visible_drift` / `tail_diag` がそれぞれ独立に means・
  anchor・opacity・scaling を引くため、8M 行では 1 回あたり数百 MB になる。
  実測で 30,000 iter の実行時間が 26.7 分（統計最小）から 32.7 分に伸びた。
  研究用途では許容範囲だが、常用するなら 1 回の転送を共有する形にまとめるべき。
- `reanchor_on_split = True` は毎イテレーションに追加のリダクションを入れるため、
  実行時間が約 30% 増える（8M cap で 27 分 → 35〜37 分）。

**3 つ目のデータセット（C）と手作業クリーンアップの解析（32 本）**

納品モデルと生成物の両方が残っていたため、専門家の手作業を正解ラベルとして
使えた。除去 76,530 行のうち 74.8% が納品箱の外で、保持側は箱の外 0 行。
残り 25.2% は被写体ごとに基準が変わるため評価から除外した。

- **入力点群のクロップが唯一の実用的な手。** 箱外スプラット −56.0%
  （効果/ノイズ 15.5）に対し、箱内 PSNR は −0.24 dB。7 秒のテキストフィルタ。
  撮影点 4,302,501 のうち 29,175 点が既に箱の外にあり、そこにスプラットが
  育っていた。アンカーはむしろその点上に行を留めるので `index` は −0.3%
  （効果/ノイズ 0.1）で効果がない。
- **`nn` + `anchor_new` は −8.6%（3.2）。** 効果は追加行を引くことだけから
  来る（`nnfree` は −0.6% で無効、`nn` 対 `nnfree` のみ効果/ノイズ 8.2）。
  切り分けの腕が無ければ原因を特定できなかった。
  副次的に、モデル全体を撮影面の 1.3 点間隔以内にクランプする
  （箱内の面までの距離 p99 が 6.61 → 1.30 点間隔）。
- **`nn` と `anchor_new` を組むときの落とし穴を修正。** `_nn_retarget` は
  `nn_refresh` iter ごとにしか走らないため、その間に追加された行は誕生位置に
  引かれる。v0.1.0 の失敗（再配置 +48%、スケール +119%）を小口で連続再現する
  ことになる。nn モードでは追加行を未アンカーで開始し、次の retarget で
  有効化するようにした（`anchor_policy.py`）。
- **学習中クロップ箱は却下。** 箱はシーンノードで CLI に無いが Python API から
  設定できる（`get_or_create_cropbox_for_splat` → `set_cropbox_data`、vec3 は
  **タプル必須**。リストは `RuntimeError('bad cast')`）。箱外は −26.1% 減るが、
  **納品する領域そのものが −3.71 dB 悪化する。**
  全画面 PSNR だけでは「背景を捨てただけ」と誤読しかねないので、
  `scripts/inbox_psnr.py`（箱の 8 頂点を各評価カメラへ投影して領域内だけを採点）
  を追加した。**領域を絞って学習する場合、全画面 PSNR は納品物の品質を測れない。**

**追加**

- `cropbox.py` — 学習中の ROI の解析と余白付け（`LFS_MPC_CROP_BOX`）
- `anchor_policy.py` — 新規行を即座にアンカーするかの判断
- `scripts/cleanup_cost.py` — 納品モデルを基準にした箱外スプラットの計数
- `scripts/inbox_psnr.py` — 納品領域に限った hold-out 測光
- `scripts/offsurface.py` — アンカーが触っていない追加行と撮影面の距離

**修正**

- **裾を `max` で読まない。** 同じ現象を 4 通りに測ると、読めるかどうかが指標の
  選び方だけで決まる: `p999` −92.6%（効果/ノイズ **199.7**）、`p99` −74.2%
  （**104.9**）、`escaped_2x` 208,899 行 → 25 行（**100.1**）に対し、
  `max` −95.7%（**1.0**）。`max` は単一行の値でベースラインのばらつきが値そのものと
  同程度になる。`drift_stats` に `p99` / `p999` を、`init_drift` に
  `escaped_1x` / `escaped_2x` / `escaped_4x` を追加。逸脱行数の閾値は不感帯ではなく
  **点群の中央値点間隔**（不感帯は腕ごとに違うので閾値にすると比較できない）。
  既存データの決着用に `scripts/rank_test.py`（厳密な並べ替え検定）を追加。
- **ランナーがログ書き込みの失敗を握り潰していた。** 別シェルの `tail -F` が
  Windows でこのファイルを掴むと `Add-Content` が毎回例外を投げる。実際に、
  13 時間まったく正常に走ったキャンペーンが最初の 2 行以降なにも記録しなかった。
  失敗時は `.alt` に退避し、一度だけ警告する。
- `mpc_tests/pytest.ini` を追加。リポジトリ直下の `__init__.py` は `lichtfeld` を
  import するプラグインのエントリポイントで、rootdir がそこだと pytest が Package
  ノードを作って収集時に import するため、**33 本全部が実行前に
  ModuleNotFoundError になっていた**（README/HANDOFF は通ると記載していた）。

## 0.2.1（0.3.0 に同梱・単独リリースなし）

LichtFeld Studio v0.5.1 のヘッドレス実行で、ドリフト統計が一切出力されず、
かつアンカーが静かに設計外の設定へ縮退していた問題の修正。どちらも実機計測で
原因を特定した（詳細は README「ヘッドレス実行時の注意」）。

**修正**

- **統計 JSON が生成されない問題。** 従来は `on_training_end` フックでのみ
  書き出していたが、v0.5.1 のヘッドレス実行はこのフックを登録するだけで
  一度も発火させず、埋め込み Python は `atexit` も実行しない。post_step
  フックの hook payload にも総 iteration 数が無いため「最終 iteration」を
  検出することもできない。`stats_snapshot_every`（既定 1000）iter ごとに
  JSON を上書きし、最後に書けたものを最終結果とする方式に変更。
  JSON に `iter` / `final` を追加し、終端との差を判別できるようにした。
- **`applied_iters` を JSON に追加。** 補正が一度でも適用されたかを示す唯一の
  指標だが、従来は終了時のログ行にしか無く、そのログ自体が出ないヘッドレス
  実行では確認手段が存在しなかった。`teleports` / `appended` / `errors` も併記。
- **`--python-script` 経路で numpy / scipy が解決されない問題。**
  LichtFeld Studio がプラグインの `.venv` を有効化するのはそのプラグインを
  ロードしたときだけで、`--python-script` には共有 venv しか渡らない。
  依存が無いと `_capture` が例外を握り潰し、`nn_spacing = 0` すなわち
  **自動不感帯なし**に落ちる。これは固定 strength と組み合わせると終盤の
  完全凍結に至る構成であり、v0.2.0 が既定値を変えて防いだはずの状態そのもの。
  `headless_anchor.py` が隣接 `.venv` を `sys.path` に追加するようにし、
  それでも解決しない場合は起動時に明示エラーを出すようにした。
- **ドリフト統計に p50 / p75 / p90 が無い問題。** 出力していたのは
  `mean` / `median` / `p95` / `max` だけだった。README は「必ず p50/p75/p90/p95 を
  併せて見る」と指示しており、v0.2.0 の見出し結果も p90 −59%（中央値は −5.5%）
  だったにもかかわらず、その指標を実行結果から再現する手段が無かった。不感帯の
  内側にいる大多数の行は自由に動くため中央値はアンカー無効時とほぼ一致し、
  リーシュの作用は上位の裾にしか現れない。`p50` / `p75` / `p90` を追加した
  （`median` は既存の結果ファイルと README が参照する名前なので `p50` の別名
  として維持）。

**検証（既定値の変更なし）**

- **別データセット B（1,749 枚 / 初期点群 7,886,332 点）で 30000 iter の
  A/B を実施**（各アーム 2 本）。既定の `free_radius_spacing = 2.0` では全指標が
  ノイズ以下だったが、原因は「効かない」ではなく **不感帯が分布の外側にあり
  作用域に何も入っていなかった**こと（ベースライン p95 / 不感帯 = 0.81）。
  1.0 に下げて比を 1.61 にすると p90 −25.6%（効果/ノイズ 25.7）、
  p95 −38.3%（同 42.3）が出て、着地点は不感帯の 0.9998 倍。測光コストは
  どちらの条件でも検出限界（0.05 dB）以下。**既定値は変更していない**
  （RAMESSES/Chest では 2.0 で p90 −59% が出ており、1 データセットで
  既定を動かす根拠にならない）。詳細は README。
- 新しいデータセットでの診断手順として
  `init_drift.p95 ÷ config.free_radius_effective` を README に追加。
  1 を下回るなら A/B しても無駄。
- `--max-cap` が初期点群より小さいと LichtFeld Studio が**ランダムに間引き**、
  さらに MRNF が `utilization 100%` で開始して密度制御が事実上停止する
  （実測: 150 iter の再配置回数が 27 対 27,744）。README に注意を追加。

**その他**

- 新規パラメータ `stats_snapshot_every`（環境変数 `LFS_MPC_SNAPSHOT_EVERY`）と
  `stats_out`（`LFS_MPC_STATS_OUT` と等価、パネル/設定からも指定可能）
- ユニットテスト 18 本追加（スナップショット判定 7、venv 探索 4、ドリフト分位 7）。
  埋め込みインタプリタ無しで実行できるよう、いずれのロジックも `lichtfeld` に
  依存しない独立モジュール（`stats_policy` / `venv_bootstrap` / `drift_stats`）に
  切り出してある。`drift_stats` が numpy を関数内で import しているのは、
  `anchor_core` が numpy 欠落時も読み込めるという既存の契約
  （numpy 使用箇所はすべて関数内 + ImportError ガード）を壊さないため

## 0.2.0 (2026-07-26)

実データ 30000 iter の A/B（独立ベースライン2本でノイズフロアを取得）と、
その結論に対する多エージェント敵対的検証の結果を反映した既定値の修正。

**破壊的変更（既定値）**

- `anchor_new_splats` を `True` → **`False`**。v0.1.0 の既定では最終モデル
  5,000,000 行のうち初期点にアンカーされていたのは 219,198 行（4.4%）だけで、
  残り 95.6% は MCMC の誕生位置・再配置着地点という幾何的意味のない座標に
  固定されていた。これが再配置 +48%、スケール膨張の主因。
- `strength` を `0.10` → **`0.01`**。0.10 は「ソフトアンカー」ではなく事実上の
  完全凍結（初期行ドリフト中央値 0.0889 → 9.65e-05）で、その代償に
  スプラット最大軸スケールが 2.2 倍（被写体領域で +97%）に膨張していた。
- `free_radius` = 0 の意味を「無効」から**「点群の中央値最近傍間隔から自動算出」**
  に変更。新パラメータ `free_radius_spacing`（既定 2.0）で倍率を指定。
  固定 `strength` + 不感帯なしの組合せは、means の実効学習率が学習中に約38倍
  変動するため終盤で必ず完全凍結に至る（構造的欠陥）。不感帯がこれを防ぐ。

**修正**

- シーン対角を生の min/max バウンディングボックスから **p1–p99 パーセンタイル**
  に変更。SfM の遠方外れ点（実測: 135 点が 37 単位の被写体を 332,130 単位の
  ボックスに拡大）で自動テレポート閾値が数千倍に誤設定され、位置ジャンプによる
  再配置検出が事実上機能していなかった。

**その他**

- ドリフト統計に `free_radius_effective` / `nn_spacing` を追加、パネルに表示
- README に実データ A/B の測定結果と、測定していないことを明記

**v0.2.0 既定の hold-out 検証（後日追記、各アーム2本 / 30000 iter / 6視点 hold-out）**

- 測光コストは検出されず: PSNR 効果 +0.089 dB に対しアーム内ばらつき 0.254 dB
  （効果/ノイズ 0.4）、SSIM も同様。0.25 dB 未満の差は判定不能という限界付き
- 幾何は選択的に効く: ドリフト中央値 −5.5%（ほぼ自由）に対し p90 −59%、
  最大 478 → 0.219。再配置回数はベースライン比 +1%（v0.1.0 既定は +48%）
- 残る代償: 初期スロット生存率 93.94% → 93.12%、スケール中央値 +4.6%
  （v0.1.0 既定の +119% から大幅縮小）

**容量を絞った条件での再現（max_cap 200万、同一4本構成）**

- 同じ結論が再現: PSNR 効果 −0.013 / アーム内ばらつき 0.259（効果/ノイズ 0.0）、
  ドリフト p90 −55%（効果/ノイズ 658）
- アーム内ばらつきは 5M 条件の 0.254 とほぼ同一 → ~0.26 dB がこのデータセットの
  固有ノイズであり、この実験系の検出限界
- アンカー2本の p90 が 0.10118 / 0.10114 と自動不感帯 0.10101 にほぼ一致。
  リーシュが設計どおり不感帯の縁で効いていることの直接確認

## 0.1.0 (2026-07-26)

初回リリース。

- dead-zone Huber 位置アンカーの proximal 補正実装（純 Python、C++ 再ビルド不要）
- 独自パラメータ: strength / free_radius / huber_delta / max_distance /
  opacity_gate / warmup_iters / start_iter / stop_iter / anchor_new_splats /
  teleport_threshold / mode(index|nn) / reference_ply / nn_refresh
- MCMC/MRNF トポロジー追跡（append 誕生アンカー・テレポート検出再アンカー）
- GUI パネル (PointCloud Anchor)、LFS_MPC_* 環境変数、--python-script 用
  ヘッドレスエントリ、終了時ドリフト統計 JSON 出力
- numpy リファレンス数式のユニットテスト 15 本
- 敵対的レビュー (16 エージェント) を経た堅牢化:
  再配置検出の不透明度二重化 (ノイズ偽陽性・閾値未満偽陰性対策)、
  min_pull_opacity (不可視行を引かない)、モデル全体変換の検出と全再キャプチャ、
  UI 操作のフラグ化 (スレッドレース根絶)、例外時の自己修復、
  ドリフト統計から再配置行を除外、nn 参照点群の不変スナップショット化と
  reference_ply 変更時の KD-tree 再構築
